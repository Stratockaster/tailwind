defmodule Tailwind.SegmentTest do
  use ExUnit.Case

  # test "normalize bearing" do
  #   assert Tailwind.Segment.normalize_bearing(50) == 50
  #   assert Tailwind.Segment.normalize_bearing(-50) == 320
  # end
end